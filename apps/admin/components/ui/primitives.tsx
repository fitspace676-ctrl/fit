// The Card / Dot / Avatar / Progress primitives now live in the shared
// @fit/ui-web design system (T1.3). Re-exported here so existing
// `@/components/ui` importers resolve unchanged.
export {
  Card,
  Dot,
  Avatar,
  Progress,
  type CardProps,
  type AvatarProps,
  type ProgressProps,
} from '@fit/ui-web';
