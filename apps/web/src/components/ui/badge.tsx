// The Badge primitive now lives in the shared @fit/ui-web design system (T1.3).
// Re-exported here so existing `@/src/components/ui` importers resolve unchanged.
export { Badge, type BadgeProps, type Tone } from '@fit/ui-web';
