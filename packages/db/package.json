{
  "name": "@fit/db",
  "version": "0.0.0",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "build": "prisma generate",
    "postinstall": "prisma generate",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate deploy",
    "db:migrate:dev": "prisma migrate dev",
    "db:studio": "prisma studio",
    "db:status": "prisma migrate status",
    "db:seed": "prisma db seed",
    "jobs:generate-instances": "tsx prisma/generate-instances.run.ts",
    "jobs:expire-credit-packs": "tsx prisma/expire-credit-packs.run.ts",
    "lint": "eslint . --max-warnings 0",
    "test": "vitest run",
    "test:watch": "vitest",
    "type-check": "tsc --noEmit",
    "lint:fix": "eslint . --fix"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.2.1",
    "rrule": "^2.8.1"
  },
  "devDependencies": {
    "@fit/config": "workspace:*",
    "@types/node": "^22.10.0",
    "prisma": "^6.2.1",
    "tsx": "^4.19.2",
    "vitest": "^3.0.0"
  }
}
