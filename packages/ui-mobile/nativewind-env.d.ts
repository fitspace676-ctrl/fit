/// <reference types="nativewind/types" />

// NativeWind augments React Native component props with `className`. Committed
// so `tsc --noEmit` type-checks this package standalone, without the Expo app
// having been started first.
