/// <reference types="nativewind/types" />

// NativeWind augments React Native component props with `className`. Committed
// (like `apps/mobile/nativewind-env.d.ts`) so `tsc --noEmit` resolves className
// styles in CI without a prior `expo start`.
