{
  "name": "@fit/types",
  "version": "0.0.0",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "build": "echo '[@fit/types] build: nothing to do yet'",
    "lint": "eslint . --max-warnings 0",
    "test": "echo '[@fit/types] test: nothing to do yet'",
    "type-check": "tsc --noEmit",
    "lint:fix": "eslint . --fix"
  },
  "devDependencies": {
    "@fit/config": "workspace:*"
  }
}
