// @fit/astryx-theme — Fit's custom Astryx theme.
//
// Import the theme object here (for the `<Theme theme={fitTheme}>` provider),
// and the compiled token CSS from the `./theme.css` export (built by
// `pnpm --filter @fit/astryx-theme build`, i.e. `astryx theme build`).

export { fitTheme, default } from './src/fitTheme';
